"""The tools Ganesh answers with, for a client that speaks MCP.

A second presentation, parallel to the routers: the same use cases, reached
from a terminal rather than from a screen. Nothing imports this package but
`src.main` — a module that came looking for a tool would have inverted the
direction the architecture runs in.
"""
