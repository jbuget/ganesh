# FastAPI and dependencies
# Held at what the MCP SDK needs: `mcp` pulls starlette 1.x, which FastAPI
# refused until 0.141. The three move together or not at all.
fastapi==0.141.1
uvicorn[standard]==0.34.0
pydantic==2.13.5
pydantic-settings==2.7.0
email-validator==2.2.0

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# Authentication & Security (Microsoft Entra ID)
python-jose[cryptography]==3.3.0

# HTTP client
httpx==0.28.1

# Tools the team reaches from a terminal client. Served at /mcp, behind the
# same keys and the same scopes as the rest.
mcp==2.2.0

# La Gazette's chapeau. The current SDK rather than WAATcher's retired
# google-generativeai: this one answers async natively, which the project asks
# for on every I/O.
google-genai==1.46.0

# Utilities
python-dotenv==1.0.1
python-dateutil==2.9.0.post0
holidays==0.64
