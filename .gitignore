# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# Env & secrets
.env
.env.*
!.env.example
# Read, not secret: the shape of the production environment, whose real values
# live in SSM Parameter Store.
!.env.production.example

# Outils & editeurs
.DS_Store
.history/
.idea/
*.swp

# Artefacts generes
server/openapi.json
client/openapi.json

# What the seed feeds the database with: the team, the missions, who works on
# what. That is the company's business, not the code's — the repository carries
# the script, never the data.
server/scripts/data/
